# 📘 **README — Учебное веб-приложение авторизации с использованием JWT (HTML + JavaScript)**

## 1. **Назначение и цель проекта**

Данное учебное веб-приложение демонстрирует базовый принцип работы клиентской авторизации на основе JWT-токена.
Приложение реализовано без использования фреймворков (React, Vue и др.) и содержит минимальный набор функций, необходимых для демонстрации:

* обработки формы входа;
* генерации тестового (фейкового) JWT-токена;
* сохранения токена в `localStorage`;
* автоматической авторизации при перезагрузке страницы;
* выполнения выхода пользователя.

Проект предназначен для изучения механики работы токен-ориентированной авторизации, а также взаимодействия веб-страницы с браузерным хранилищем.

---

## 2. **Основные возможности приложения**

### 2.1. **Вход пользователя**

Пользователь вводит e-mail и пароль в форму.
При нажатии кнопки *«Войти»* приложение:

1. генерирует фейковый JWT-токен;
2. сохраняет его в `localStorage`;
3. переключает интерфейс на защищённый экран «Вы вошли».

### 2.2. **Генерация тестового токена**

Для учебных целей используется токен вида:

```
fake-jwt-<base64 от email + timestamp>
```

Это не настоящий JWT, а лишь его имитация.

### 2.3. **Сохранение токена**

Токен сохраняется в браузерном хранилище:

```javascript
localStorage.setItem("jwt_token", token);
```

Благодаря этому при обновлении страницы пользователь остаётся авторизованным.

### 2.4. **Автоматический вход**

При загрузке приложения выполняется проверка:

```javascript
localStorage.getItem("jwt_token");
```

Если токен найден — показывается защищённый интерфейс.

### 2.5. **Выход из системы**

Нажатие кнопки *«Выход»* приводит к:

* удалению токена из `localStorage`;
* возврату к форме входа.

---

## 3. **Структура проекта**

Проект состоит из трёх файлов:

```
index.html   — основной HTML-файл приложения
style.css    — стили оформления (необязательные, но улучшают визуализацию)
app.js       — логика приложения (проверка токена, вход, выход)
```

### 3.1. **index.html**

Содержит контейнер для динамического интерфейса:

```html
<div id="app"></div>
```

и подключение скрипта `app.js`.

### 3.2. **style.css**

Содержит базовые стили для формы, кнопок и контейнеров, чтобы приложения выглядело аккуратно.

### 3.3. **app.js**

Основные функции:

* `showLoginForm()` — рендер формы входа;
* `showLoggedIn()` — рендер экрана авторизованного пользователя;
* проверка токена при загрузке приложения;
* генерация фейкового токена;
* логика `login` и `logout`.

---

## 4. **Скриншоты работы приложения**

Ниже приведены места для вставки скриншотов:

### 4.1. **Форма входа**

```
<img width="1366" height="768" alt="Снимок экрана (9)" src="https://github.com/user-attachments/assets/9979ebf7-77e3-4c1a-b423-8072e2d8fa66" />

```

### 4.2. **Экран авторизованного пользователя**

```
<img width="1366" height="768" alt="Снимок экрана (11)" src="https://github.com/user-attachments/assets/54ed5447-85bf-4ed6-ad53-ae0f89df3c83" />

---

## 5. **Инструкции по запуску**

1. Скопировать файлы `index.html`, `style.css`, `app.js` в одну папку.
2. Открыть файл **index.html** в браузере (двойной клик или «Открыть с помощью»).
3. Приложение работает локально без дополнительной настройки.

---


## 6. **Заключение**

Приложение демонстрирует базовый подход к авторизации на стороне клиента и является простым примером для понимания работы JWT-подобной логики.
Этот проект подходит для лабораторных работ, домашних заданий и учебных демонстраций.
